public enum GameDiff {
	Easy,
	Hard,
	Ultra
}

public enum LinkMode {
	Basic,
	Advance
}

public enum AdvanceLinkMode {
	L,
	J,
	CTM
}

public enum Direction {
	Up,
	Down,
	Left,
	Right
}

public enum LineMode {
	Show,
	Hide
}

public enum ShowMode {
	Now,
	After
}

public enum GameMode {
	Mission,
	Free,
	Custom,
	Challenge
}

public enum ChallengeMode {
	Easy,
	Middle,
	Hard
}